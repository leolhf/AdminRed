// version.js
// Número de versión de la app. Súbelo cada vez que subas cambios a GitHub.
// Se usa para: 1) mostrarlo en el header, 2) invalidar la caché del Service Worker (sw.js).
// No depende de nada. Debe cargarse PRIMERO (antes que todo, incluido sw.js).
const APP_VERSION = '4.1.0';
